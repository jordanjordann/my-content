# Handoff — my-content (2026-07-23)

**Session:** 2026-07-23
**Repo:** `/Users/jordanatha/Desktop/Projects/my-content` · github: `jordanjordann/my-content`
**Branch:** `main`, at `40c09f7`. **One open PR:** #92 (`be/69-pipeline-read-api-migration-007`) — open, unmerged, on purpose (see below).

Supersedes `docs/HANDOFF-2026-07-22-session2.md`, which now carries a pointer at the top. Do not plan from it.

---

## Read these first — do not re-derive

| Path | What it holds |
|---|---|
| `docs/HANDOFF-2026-07-22-session2.md` | Prior session's handoff. Still useful for the product paragraph, deferred items, and orchestration lessons — but its "ready/next chain" and commit references are stale. Read this doc first. |
| `docs/RUNBOOK.md` | **STALE as of this session — see traps.** Header claims verified against `f181f53`; main is six commits past that. Test count (85) is stale (now 105). Migration section (stops at 006) is still accurate. |
| `.claude/context/verified-facts.md` | Live-verified ScrapeCreators + Gemini facts. **Append, never clobber.** Contains one uncorrected inaccuracy carried over from this session — see traps. |
| `docs/PRD-analysis-schema-redesign.md` | The settled analysis contract. |
| `docs/TDD-analysis-schema-redesign.md` | Implementation design, §8.1 governs the #69/#70 same-deploy dependency. |
| `docs/design/DESIGN-analysis-tier1-style-section.md` | Confirmed UI direction A (tabbed). #70 implements it. Still not started. |

Ticket detail is in GitHub issues — `gh issue list`. Don't restate them.

---

## First thing to tell the owner

**#92 (#69's PR) is built, self-verified, but has not had its own dedicated code-reviewer pass, and is not merged.** Merging it alone breaks `npm run build` on main, because three FE files under #70's scope (`AnalysisDataTable.tsx`, `AnalysisScorecardSection.tsx`, `AnalysisPatternsSection.tsx`) still reference the old Scorecard/Patterns shape that #68 deleted. This is the known, planned, documented consequence of landing #69 before #70 — not a defect. Next step is a review pass on #92, then start #70, then merge both together.

Second thing: **worktree cleanup is in progress and the owner took it over personally this session.** Outcome unknown as of this handoff — run `git worktree list` fresh, do not assume either "still ~31" or "cleaned up."

Third thing: **RUNBOOK is stale and was deliberately not fixed this session** due to budget. Flag it, don't let it rot further.

---

## Product, in one paragraph

Content analysis tool for an Indonesian social-media/marketing agency (the user is the agency, not the creator). Paste an Instagram Reel or YouTube Short → ScrapeCreators fetches metadata → video downloaded → Gemini watches and analyses → stored. Destination is a content brief generator: name a topic, get a brief written in a specific creator's style learned from ≥5 of their analysed videos. Not a calendar, not a scheduler. Output language Indonesian; UI English. Desktop dashboard.

---

## Starting state (this session)

`main` was at `e42b7cb` (session 2's handoff commit). Zero open PRs. ~85 tests. EOL Gemini SDK already gone, 6 real Instagram fixtures, ~31,994 ScrapeCreators credits.

---

## What shipped this session — five merges, all on main, in order

| Commit | PR / ticket | |
|---|---|---|
| `35ef5c8` | #87 / #66 | Gemini structured output: `responseSchema` + temperature 0 + token budget |
| `ca6a570` | #88 / #67 | Prompt rewrite: 5-band rubrics, Indonesian few-shots, discriminator rules |
| `d0c1ca1` | #89 / #68 | Parser + validation rewrite: loud failure, no fabricated scores |
| `3da0df1` | #90 / #57 | YouTube profile resolution: subscriber count, engagement rate, remove Instagram-only pipeline guard |
| `40c09f7` | #91 / #83 | GitHub Actions CI: test + typecheck + lint + build, hard no-live-API-calls guarantee |

### #66 → PR #87 — Gemini structured output

New `lib/server/analysis/schema/responseSchema.ts`: `ANALYSIS_RESPONSE_SCHEMA` with every enum spread from the taxonomy module, `hookTypeSecondary` required-non-nullable with a wire-only `"NONE"` sentinel, `schemaVersion` deliberately excluded (stamped server-side). `gemini/generate.ts`: temperature 0, `responseMimeType: application/json`, `responseSchema` wired, `maxOutputTokens` raised 8192→32768, unconditional `usageMetadata` logging, throws on any non-`STOP` `finishReason` before parsing.

**One live Gemini call was made this session** (owner-authorized, one-time exception) — `finishReason: STOP`, usage 68 prompt + 329 output + 696 thinking = 1093 total tokens, logged in `.claude/context/verified-facts.md`.

**Important caveat, verified by review:** that live call exercised SDK mechanics via the OLD ported probe harness (`structured-output-baseline.mjs`, its own probe schema, temperature 0.2) — **not** the actual production `ANALYSIS_RESPONSE_SCHEMA` at temperature 0. The production schema itself remains typecheck-verified only, never live-exercised end-to-end.

A misleading code comment (a fabricated-looking "38 output + 48 thinking → MAX_TOKENS" stat that doesn't mathematically support 8192 exhaustion) was found duplicated in `verified-facts.md` and was corrected **in code only** (softened to describe the mechanism without the fake stat). **The `verified-facts.md` copy was not corrected** — it's an append-only file and nobody appended a fix — and remains a live inaccuracy in that file as of this handoff.

Re-verified independently post-merge with real shell: 85/85 tests, tsc clean, lint clean, build clean.

### #67 → PR #88 — Prompt rewrite

New `lib/server/analysis/prompts/rubrics.ts` (anchored 5-band rubrics for 7 scorecard dimensions, typed as fixed 5-tuples — a missing band is a compile error) and `taxonomyPrompt.ts` (generated from the taxonomy module, zero hand-duplicated enum lists). `system.ts` rewritten to pure assembly; the old hand-written JSON-shape block deleted.

All discriminator rules from the PRD confirmed present: both `hookType` collision pairs, all six `formatArchetype` pairs, both `ctaType` anchors, `hasAudienceCallout` orthogonality note, `ctaType`/`ctaTiming` NONE biconditional. No file overlap with #66.

A flagged concern — `hookTypeSecondary` `"NONE"` vs `null` mismatch between #66 and #67 — was investigated with a direct side-by-side file read and confirmed a **non-issue**: #66's schema genuinely accepts `"NONE"` as a wire enum value, #67's prompt correctly emits it, they agree.

Re-verified with real shell: 85/85, tsc/lint/build clean.

### #68 → PR #89 — Parser + validation rewrite

Full rewrite of `lib/server/analysis/types/analysis.ts` (new 7-key `Scorecard`: `hookStrength`, `retentionFlow`, `visualPolish`, `ctaEffectiveness`, `messageClarity`, `originality`, `emotionalResonance`; old 1–10 shape and `Patterns` type deleted entirely) and `lib/server/analysis/parser/validation.ts` (every check throws `AnalysisValidationError` with a field path).

Verified by review to have **zero defaults or clamping** — grepped for `??`, `Math.min/max`, `clamp`, `||`, found nothing. Out-of-range values are rejected, not coerced. `parser/analysis.ts`: `extractJson()` deleted, direct `JSON.parse`, `SyntaxError` propagates unchanged (no repair path). Wire `"NONE"` → domain `null` normalization for `hookTypeSecondary` implemented and tested. `rubrics.ts` updated to import `ScorecardDimension = keyof Scorecard` instead of a local duplicate type, closing a flagged cross-ticket drift risk.

21 new tests (105 total after this merge). Real shell verification: 105/105, tsc/lint clean, build clean.

**Known gap, flagged by review, not resolved by #68 itself:** `lib/api/analyses/types.ts` (API-layer type) and analysis-detail UI components (`AnalysisPatternsSection`, confirmed real path `app/app/analyses/components/sections/AnalysisPatternsSection/`) still expect the old shape — new analyses persisted after this merge have a shape the current UI cannot render correctly. This is a known, planned gap, confirmed via reading the full ticket bodies to be explicitly owned by #69 (api-layer) and #70 (FE: rename+rewrite `AnalysisPatternsSection` → `AnalysisRedFlagsSection`), which are already sequenced next and already have an explicit mutual "must ship in the same deploy" dependency documented in their own ticket bodies. Not new, not unticketed — just now confirmed real and currently live on main.

### #57 → PR #90 — YouTube profile resolution

Ticket's own issue-body banner said "STATUS: BLOCKED — do not start" citing unmerged #53/#54 and ~10 credits — **verified stale** via git log (both merged: #53 at `0798176`, #54 at `7c11ccb`) before proceeding. Same label/banner-drift pattern flagged in session 2's handoff, now recurred.

`lib/server/profiles/service.ts`: split into `fetchInstagramProfileInput` (verified verbatim/unchanged) and new `fetchYoutubeChannelInput` mapping `subscriberCount`→`followerCount`, `channelId`→`externalId`, `name`→`fullName`, `description`→`biography`; `followingCount`/`isBusinessAccount`/`isPrivate` correctly left `null` (verified not coerced to `false`, all the way through the DB round-trip via `toNullableBoolInt`/`toNullableBoolean`). Handle normalization (strip `@`, lowercase) verified applied consistently to both cache-key lookup and outbound API call — no cache-mismatch risk. `pipeline/index.ts`: Instagram-only guard removed, `resolveProfile` now called unconditionally; Instagram path verified byte-identical to before.

A follow-up commit (`e9121ea`) added `profilePicUrl` mapping from `raw.avatar?.image?.sources?.[0]?.url ?? null` (owner-requested after noticing it was hardcoded null) — verified against the actual fixture (exactly one 68x68 source, not an arbitrary pick from a larger array), verified the fallback chain cannot throw on any missing-field combination, verified `rawPayload: JSON.stringify(raw)` genuinely stores the entire unmodified channel API response with zero stripping (avatar, plus incidentally a public contact email + country, are all in `profiles.raw_payload` verbatim — not surfaced as columns, but not lost either).

Real shell verification both before and after the fix: 85/85 (predates #68's +20 tests), tsc/lint/build clean.

**Not done, explicitly flagged by its own developer:** the ticket's manual DB/live-analysis smoke tests (new profiles row populated with `follower_count`, cache-hit behavior on re-analysis, graceful degradation on channel-fetch failure) were never run — no live-call authorization was granted for this ticket.

### #83 → PR #91 — GitHub Actions CI

Same stale-banner pattern as #57 (banner said blocked on PR #81, which was actually already merged) — verified and proceeded. New `.github/workflows/ci.yml` (triggers on PR to any branch + push to main; runs test → typecheck → lint → build), `.nvmrc` pinned to 24.14.1, new `typecheck` npm script (`tsc --noEmit`), RUNBOOK updated.

Discovered the ticket's suggested network-blocking mechanism (`noNetwork.ts`) already existed under a different name from PR #81 (`blockLiveFetch.ts`) — did not duplicate it. The "hard" no-live-API-calls guarantee is two-layered: (1) existing test-level fetch stub, (2) CI-level — confirmed via `gh secret list` that neither `GEMINI_API_KEY` nor `SCRAPECREATORS_API_KEY` exist as repo secrets, so even a stub-bypassing bug has nothing to authenticate against.

**This claim was proven, not just asserted:** the developer pushed a temporary commit containing a live unstubbed `fetch()` call, confirmed CI actually went red on the exact guard error (run `29976661837`), then reverted and confirmed green (run `29976746463`). This is the first real CI run in the project's history. Post-merge, CI ran on the merge commit itself (`40c09f7`) and passed (run `29977007440`).

---

## What did NOT merge — deliberately held back

**#69 → PR #92** ("[BE] Pipeline + read API wiring for the new contract, migration 007") — branch `be/69-pipeline-read-api-migration-007`, **OPEN, NOT MERGED, on purpose.**

New migration `migrations/007_add_schema_version.sql` (additive: one column `schema_version INTEGER` on `analyses`, one index `idx_analyses_schema_version`) — applied and verified locally via `npm run db:migrate` + `PRAGMA table_info`. `app/api/analyses/route.ts`: inline dimension-list cast deleted, now imports `ContentAnalysis` from the shared type, `schemaVersion` surfaced on list items. `lib/api/analyses/types.ts`: rewritten to mirror new `ContentAnalysis` exactly, taxonomy types re-exported (not restated) from `lib/analysis/taxonomy`, `Patterns` deleted. `lib/server/db.ts`: `schema_version` added to both `getAnalysesList()` and `getAnalysisDetail()` SELECT lists. `pipeline/index.ts` persists `schema_version` in the same UPDATE as `result_content`, keeping the column and the JSON's internal `schemaVersion` in agreement.

Real verification: `db:migrate` applied cleanly, tsc clean for everything in #69's own scope, lint clean, 105/105 tests pass.

**`npm run build` currently fails** — but only in three files owned by #70 (`AnalysisDataTable.tsx`, `AnalysisScorecardSection.tsx`, `AnalysisPatternsSection.tsx`, which still reference the now-deleted old Scorecard/Patterns shape); backend code itself compiles. This was flagged clearly in the PR description as the known, planned consequence of #69 landing before #70 (per TDD §8.1) — not a defect in #69's own code.

**This is why it wasn't merged this session:** merging #69 alone would leave main's build broken until #70 (not started) lands.

**#92 itself has not been through its own dedicated code-reviewer pass this session** — that's outstanding.

---

## Worktree situation — IN PROGRESS, owner handling personally

Session started with 25 worktrees (2 dangling ones pruned automatically early via `git worktree prune`). By end of session, accumulated to 32 non-main worktrees (many created by this session's own dispatched agents).

Two automated cleanup attempts both **stalled** (killed after 600s no-progress) — root cause identified as `git status`/interactive git commands hanging when run inside individual worktree directories (likely a credential-helper or auth prompt blocking with no terminal to answer it — unconfirmed root cause, but reproducible symptom).

One stalled agent's dying message made an unverified/likely-false claim about "disabling the sandbox" — treated as not credible, no evidence it did anything real.

A read-only re-verification confirmed: 31 worktrees remained after the stalls (only 1 of 32 actually vanished, identity unconfirmed), all 31 structurally intact (no corruption, no half-deleted state), plus 4 unrelated orphaned directories found that were never valid git worktrees at all (`agent-a39e4ecad32fd3f73`, `agent-a4d99fb47e4228530`, `pr26`, `pr52build` — safe to `rm -rf` directly, git doesn't track them).

**The owner has taken over worktree cleanup personally as of this handoff** — was given direct shell commands to run (`git branch -vv` to check push status first, then a loop of `git worktree remove <path> --force` + `git worktree prune`, avoiding the hang by not running `git status` per-worktree). **Outcome of that personal cleanup is unknown as of this handoff** — next session should run `git worktree list` fresh to see current state, do not assume either "still 31" or "cleaned up" without checking.

---

## Documentation staleness — NOT updated this session, deliberately deferred

`docs/RUNBOOK.md` header says "Verified against main at `f181f53`" — now six commits stale (main is at `40c09f7` as of this handoff, will be different again once #92/#69 merges). Its stated test count (85) is stale (currently 105 after #68). Its migration section (stops at 006) is still **accurate** as written since 007 is only on the unmerged #92 branch, not on main.

This was a deliberate decision to not spend a session-ending agent cycle on RUNBOOK reconciliation, given budget constraints — **flag prominently as first-thing-to-fix, do not let it silently rot further.**

---

## The traps that will bite

1. **`.claude/context/verified-facts.md` still contains the fabricated-looking "38 output + 48 thinking → MAX_TOKENS" stat**, uncorrected (append-only, nobody appended a fix). The code copy of this comment was softened; the doc copy was not. Don't cite this stat as verified — it isn't.
2. **The live Gemini call this session did not exercise the production schema.** It ran the old ported probe harness (`structured-output-baseline.mjs`) at temperature 0.2 with its own probe schema — not `ANALYSIS_RESPONSE_SCHEMA` at temperature 0. Treat #66's production schema as typecheck-verified only, not live-verified, until a real end-to-end call is made against it.
3. **Ticket status banners keep being stale.** #57 and #83 both had "BLOCKED" banners this session that were actually cleared — verify blockers against `git log`, not the issue body, before treating a ticket as blocked. This is the second session running this pattern has recurred.
4. **`npm run build` is currently broken on `be/69-pipeline-read-api-migration-007`** by design, in exactly three FE files. Do not "fix" this by patching those files ad hoc — #70 is the ticket that rewrites them properly, and #69+#70 must merge in the same deploy per TDD §8.1.
5. **RUNBOOK's header/test-count are stale; its migration section is not.** Don't trust the header timestamp as a proxy for the whole document's accuracy — check section by section.
6. **Worktree count and identity are unknown right now.** Do not assume the owner's personal cleanup succeeded, partially succeeded, or didn't happen. Check `git worktree list` before doing anything worktree-related.
7. **`my-content.db` is tracked in git despite being gitignored** (carried over from session 2 — confirmed still true; it shows modified in this session's git status). Do not commit it.

---

## Open questions for the owner — carried over, still unanswered, do not relitigate as "new"

- Do co-authored posts count toward a creator's style fingerprint? (blocks #72 eventually, not urgent yet since #72 is later in the chain, still not ticketed per session 2's deliberate choice)
- The username split-account bug from #82 (session 2) — still needs a ticket, not created.
- `credits_charged` / spend visibility — still nothing tracks it.
- ScrapeCreators key rotation — still deferred, key still exposed in plaintext from an earlier session.
- #69's own "Legacy rows" note: if the ~2 existing legacy analysis rows are kept rather than deleted, the UI needs to branch on `schemaVersion` to render them, and neither #69 nor #70 concretely scopes that UI-side branching — worth a one-line confirmation from whoever picks up #69/#70, likely moot since RUNBOOK describes the DB as "effectively empty" (1 row) with no backfill budget, but not yet explicitly closed out.

---

## The ready/next chain as of this handoff

Everything merged this session (#66, #67, #68, #57, #83) is **done, do not re-dispatch.**

**#69/PR#92 is built, self-verified, not yet peer-reviewed by a dedicated code-reviewer pass, and not merged — merging it alone breaks the build.**

The clear next step is:
1. Get #92 a proper code-reviewer pass.
2. Start #70 (FE — Direction A tabbed design, already has an approved design spec from session 2 at `docs/design/DESIGN-analysis-tier1-style-section.md`).
3. Merge #69 and #70 together, same deploy, per their own stated mutual dependency.

After that: #71 (carousel, blocked on #69 landing so migration 008 can be written against a known 007 column list) and #72 (fingerprint, blocked on the still-open co-author question above) run in parallel. Then #73 (fingerprint API), #74 (QA).

---

## Model/orchestration facts

Every dispatched agent this session ran on its configured frontmatter default (sonnet for backend-developer and code-reviewer) — zero `model` parameter overrides, per the standing hard rule. The max-2-concurrent-agents rule was maintained throughout except during merge-only tasks (which ran solo). All merges were explicitly approved by the owner in the conversation before being dispatched to a merge-only agent — no agent merged unilaterally.

---

## Suggested skills for the next session

- **`review`** or `code-review`-equivalent — #92 needs its own dedicated review pass before anything else happens.
- **`frontend-design`** or **`ui-ux-pro-max`** — for #70, Direction A tabbed modal, design spec already approved.
- **`create-pull-request`** / **`create-commit`** — routine, high volume, unchanged from session 2.
- **`retrospective`** — the recurring stale-banner pattern (now twice) and the worktree-cleanup stall both deserve formalising, same as the agent-model issue and merge-policy gap flagged in session 2.

---

## Secrets

`SCRAPECREATORS_API_KEY` and `GEMINI_API_KEY` live in `.env.local` (gitignored, untracked). No keys are reproduced in this document. The ScrapeCreators key was pasted in plaintext in an earlier chat session and rotation is still deferred — it remains exposed.
