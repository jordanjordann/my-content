/**
 * Tailwind v4 default breakpoints (in px). `app/globals.css` defines no
 * `--breakpoint-*` override, so these values are exact and must stay in
 * lockstep with Tailwind's defaults for as long as that remains true.
 */
export const BREAKPOINT_PX = { sm: 640, md: 768, lg: 1024 } as const;
