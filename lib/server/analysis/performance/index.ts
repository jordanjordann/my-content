// Barrel — only re-exports, no implementation (AGENTS.md module conventions).
// Grows as 3B-2 onward add reach.ts, availability.ts, baseline.ts,
// judgement.ts, computeBlock.ts, types.ts, constants.ts (TDD §2).
export { computeEngagementRate } from "./ratios";
export type { FollowerDenominatedRatio } from "./ratios";
