/**
 * DESIGN-3C §6.3 — verbatim, including the doc's own apostrophe usage (not "corrected"; this
 * exact string is the approved copy). Content, Performance, and both engagement columns are
 * locked (R-12.3.1 — hiding a denominator-bearing column is how a *user* violates it).
 */
export const LOCKED_COLUMN_TOOLTIP =
  "Always shown — this column carries information the numbers can't be read without.";
