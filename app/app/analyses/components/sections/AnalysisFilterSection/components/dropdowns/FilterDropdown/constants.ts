/** Panel width for Platform/Status — no search field, shorter fixed option lists (design §2). */
export const FILTER_DROPDOWN_PANEL_WIDTH = "w-52";

/** Panel width for Account — needs room for the search field and monospace account handles. */
export const FILTER_DROPDOWN_PANEL_WIDTH_WITH_SEARCH = "w-64";

/** Max height of the scrollable option list — keeps a long Account list from pushing page height. */
export const FILTER_DROPDOWN_OPTION_LIST_MAX_HEIGHT = "max-h-52";
